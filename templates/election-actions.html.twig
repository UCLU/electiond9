{% if (actions|length) > 0 %}
  <p class="election_actions_container election__actions_container">
    {% for action in actions %}
      <a class="btn button button--{{
        action.button_type
        }} election_action election__action"
        href="{{ action.link }}">
        {{ action.title }}
      </a>
    {% endfor %}
  </p>
{% endif %}
